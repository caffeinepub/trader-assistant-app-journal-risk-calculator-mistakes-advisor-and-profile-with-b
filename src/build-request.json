{
  "kind": "build_request",
  "title": "Restore application to Version 35",
  "priority": "normal",
  "requirements": [
    {
      "id": "REQ-1",
      "text": "Restore the deployed application codebase to match the previously working **Version 35** state (full rollback), so the app behavior and UI match Version 35 rather than the current version.",
      "target": "both",
      "source": {
        "messageIds": [
          "msg-6",
          "msg-12"
        ],
        "quotes": [
          "Pls give me version 35 back",
          "Yes"
        ]
      },
      "acceptanceCriteria": [
        "After deployment, the running app reflects the same functionality and UI as Version 35 (no Version 38+ changes remain).",
        "No build-time or deploy-time errors occur during the restore attempt."
      ]
    },
    {
      "id": "REQ-2",
      "text": "Fix the restore/rollback workflow so the Version 35 restore succeeds (the previous restore attempt failed); ensure the build pipeline can complete a Version 35 restore without throwing an implementation error.",
      "target": "unknown",
      "source": {
        "messageIds": [
          "msg-8",
          "msg-6"
        ],
        "quotes": [
          "Sorry, the AI was unable to implement your request.",
          "Pls give me version 35 back"
        ]
      },
      "acceptanceCriteria": [
        "Triggering a restore to Version 35 completes successfully end-to-end (build + deploy).",
        "If any restore preconditions are required, the system surfaces a clear, actionable error message explaining what is missing instead of a generic implementation failure."
      ]
    }
  ],
  "constraints": [
    "Backend must remain a single Motoko actor in backend/main.mo; only create backend/migration.mo if an upgrade requires state migration.",
    "Do not modify any files under frontend/src/components/ui or any paths listed as immutable in SYSTEM_CONTEXT."
  ],
  "nonGoals": [
    "Implementing new features or redesigning the app beyond what is necessary to match Version 35.",
    "Changing authentication providers (Internet Identity must remain the auth solution)."
  ],
  "imageRequirements": {
    "required": [],
    "edits": []
  },
  "userProfileUpdate": {
    "goalsToAdd": [],
    "goalsToRemove": [],
    "preferencesToAdd": [],
    "preferencesToRemove": [],
    "miscToAdd": [],
    "miscToRemove": [],
    "fieldsToSet": {},
    "fieldsToDelete": []
  }
}